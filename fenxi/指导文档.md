# 执行说明

在 **`c:\Users\admin\Desktop\fenxi`** 下打开终端，按需执行。

---

## 只想重新生成两篇分析文档（数据没变）

会更新：`_前200只股票_大涨归纳_四段式.md`、`_前200只股票_主线按题材归纳表.md`

```powershell
cd c:\Users\admin\Desktop\fenxi
python build_top200_surge_narrative.py
```

若 `python` 不可用，把 `python` 换成你本机 Python 的路径。

---

## 换了 Excel / 要先从解压后的表刷新股票列表

前提：已把 xlsx 解压到 `_xlsx_extract`（且 `sheet1.xml` 是你要的那张表）。

先写 `_top200_stocks.json`，再生成上面两篇 md：

```powershell
cd c:\Users\admin\Desktop\fenxi
python _parse_top200.py
python build_top200_surge_narrative.py
```

---

说明：正文里的「为何涨 / 主线」是题材归纳，不是公告级考证；投资请以交易所与上市公司披露为准。
